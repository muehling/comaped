<div class="row">
  <div class = "col-md-2"></div>
  <div class = "col-md-8">
    <div class="panel panel-default">
      <div class="panel-body">

        <table class = "table">
          <tbody>
            <tr><td><%= t('email') %></td><td><%= @user.email %></td></tr>
            <tr><td><%= t('projects.text') %></td><td><%= @user.projects.count %></td></tr>
            <tr><td><%= t('maps') %></td><td><%= @user.concept_map_count %></td></tr>
          </tbody>
        </table>
        <%= link_to t('.change'), edit_user_path(@user), class: 'btn btn-default'%>

      </div>
    </div>
  </div>
  <div class = "col-md-2"></div>
</div>
