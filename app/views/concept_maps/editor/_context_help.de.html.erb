<p id="ch_normal" class="hidden">
  <% if @concept_map.survey.concept_labels.blank? %>Neues Konzept: Langer Klick auf Zeichenfläche.<% end %> Ausschnitt verändern: Klicken und ziehen, scrollen (oder Knöpfe auf der Zeichenfläche)
</p>

<p id="ch_hovernode" class="hidden">
Klicken und ziehen um Konzept zu verschieben, <% if @concept_map.survey.concept_labels.blank? %>klicken um Konzept zu bearbeiten oder zu löschen,<% end %> langer Klick um Verbindung anzulegen
</p>

<p id="ch_hoveredge" class="hidden">
  Klicken um Verbindung zu bearbeiten oder zu löschen
</p>

<p id="ch_edit" class="hidden">
  Neuen Bezeichner eingeben, dann <span class="glyphicon glyphicon-ok"></span> oder Enter. Abbrechen mit <span class="glyphicon glyphicon-remove"></span> oder Esc oder Klick auf Zeichenfläche. Löschen mit <span class="glyphicon glyphicon-trash"></span> (<strong>Achtung:</strong> Kein Rückgängig!)
</p>

<p id="ch_new" class="hidden">
  Bezeichner eingeben und <span class="glyphicon glyphicon-ok"></span> oder Enter, abbrechen mit <span class="glyphicon glyphicon-remove"></span> oder Esc oder Klick auf Zeichenfläche
</p>

<p id="ch_addedge" class="hidden">
  Klick auf Zielkonzept um Verbindung anzulegen oder auf Zeichenfläche um abzubrechen
</p>