development:
  adapter: async

test:
  adapter: async

production:
  adapter: redis
  url: <%= ENV.fetch('REDIS_URL') {'random_default'}%>
  channel_prefix: 'comaped_production'

staging:
  adapter: redis
  url: <%= ENV.fetch('REDIS_URL') {'random_default'}%>
  channel_prefix: 'comaped_staging'
